-- Selecionar tudo da tabela DimStore
SELECT 
	*
FROM 
	DimStore

-- Selecionar determinadas colunas da tabela DimStore (ID, Nome e Numero de Telefone)
/*SELECT
	StoreKey,
	StoreName,
	StorePhone
FROM
	DimStore*/

SELECT * FROM DimProduct
